import 'package:flutter/material.dart';

class SellersRevenueScreen extends StatelessWidget {
  const SellersRevenueScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Revenue')),
      body: Center(child: Text('Revenue page coming soon...')),
    );
  }
}
